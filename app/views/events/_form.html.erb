<%= form_for @event do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
          <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<form>
  <fieldset class="field form-group">
    <%= f.label :name %><br>
      <%= f.text_field :name, class:'form-control' %>
        </fieldset>
  <fieldset class="field form-group">
    <%= f.label :extended_html_description %><br>
      <%= f.text_area :extended_html_description, class:'form-control content-field-body' %>
        </fieldset>

        <%= f.label :starts_at%>
        <%= f.text_field :starts_at%>

        <%= f.label :ends_at %>
        <%= f.text_field :ends_at%>

        <%= f.select :category_id, Category.all.map{|e| [e.name, e.id]} %>

        <%= f.select :venue_id, Venue.all.map{|e| [e.name, e.id]} %>`

  <div class="actions">
      <%= f.submit %>
  </div>

</form>



<% end %>
